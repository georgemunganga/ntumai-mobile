/**
 * This project now uses Expo Router as the entry point.
 * The App component remains to satisfy Expo's registerRootComponent helper,
 * but it simply renders nothing because routing is handled via the app directory.
 */
export default function App() {
  return null;
}
